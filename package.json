{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-cv": "node scripts/generate-cv.js",
    "generate-dossier": "node scripts/generate-dossier.js",
    "predev": "node scripts/render-home.mjs",
    "prebuild": "node scripts/render-home.mjs"
  },
  "devDependencies": {
    "puppeteer": "^25.2.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.185.0",
    "vercel": "^54.18.6"
  }
}
